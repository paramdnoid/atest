apiVersion: v1
kind: Service
metadata:
  name: zunftgewerk-loki
  namespace: zunftgewerk
spec:
  selector:
    app: zunftgewerk-loki
  ports:
    - name: http
      protocol: TCP
      port: 3100
      targetPort: 3100
