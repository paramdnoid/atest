apiVersion: v1
kind: Service
metadata:
  name: zunftgewerk-api
  namespace: zunftgewerk
spec:
  selector:
    app: zunftgewerk-api
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
    - name: grpc
      protocol: TCP
      port: 9090
      targetPort: 9090
