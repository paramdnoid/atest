apiVersion: v1
kind: Service
metadata:
  name: zunftgewerk-api
  namespace: zunftgewerk
spec:
  selector:
    app: zunftgewerk-api
  ports:
    - name: grpc
      protocol: TCP
      port: 9090
      targetPort: 9090
