apiVersion: v1
kind: Service
metadata:
  name: zunftgewerk-web
  namespace: zunftgewerk
spec:
  selector:
    app: zunftgewerk-web
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: 3001
