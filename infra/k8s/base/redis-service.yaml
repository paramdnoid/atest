apiVersion: v1
kind: Service
metadata:
  name: zunftgewerk-redis
  namespace: zunftgewerk
spec:
  type: ClusterIP
  selector:
    app: zunftgewerk-redis
  ports:
    - port: 6379
      targetPort: 6379
