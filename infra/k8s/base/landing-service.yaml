apiVersion: v1
kind: Service
metadata:
  name: zunftgewerk-landing
  namespace: zunftgewerk
spec:
  selector:
    app: zunftgewerk-landing
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
